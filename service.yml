apiVersion: v1
kind: Service
metadata:
  name: insurance-service
  labels:
    app: insurance
spec:
  ports:
   - name: akash-insurance-container
     port: 8080
     targetPort: 8080
  type: NodePort
  selector:
    app: insurance
    tier: web
